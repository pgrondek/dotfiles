# basic configuration
backend = "glx";
vsync = true;
glx-no-stencil = true;
panting-on-overlay = true;

unredir-if-possible = true;

opengl-swc = true;

# Fading
fading = false;
fade-in-step = 0.05;
fade-out-step = 0.05;

# Corners
corner-radius = 10;
round-borders = 2;
shadow-opacity = 1;
detect-rounded-corners = false;
round-borders-rule = [
  "0:class_g      = 'Polybar'",
  "3:class_g      = 'XTerm'",
  "3:class_g      = 'URxvt'",
  "15:class_g     = 'Signal'"
];
rounded-corners-exclude = [
    "class_g = 'Polybar'",
    "class_g = 'dwmtray'",
    "class_g = 'dwm'",
    "class_g = 'dmenu'",
    "class_g = 'Dunst'",
    "name = 'scratchpad'"
]
