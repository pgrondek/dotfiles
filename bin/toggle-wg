#!/usr/bin/env bash

status=$(systemctl is-active wg-quick@wg0.service)
SUDO="pkexec --disable-internal-agent"

if [ "$status" = "active" ]; then
  $SUDO systemctl stop wg-quick@wg0.service;
else
  $SUDO systemctl start wg-quick@wg0.service;
fi
